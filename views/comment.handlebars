<div class="card">
  <div class="card-header">
 Edit Post
  </div>
  <div class="card-body">
    <form class="form update-form">
      <div class="form-group">
        <label for="title">Title:</label>
        <input class="form-input" type="text" id="blog-name" value="{{name}}" />
      </div>
      <div class="form-group">
        <label for="blog">Content:</label>
        <input class="form-input" type="text" id="blog-content" value="{{description}}" />
      </div>
      <div class="form-group">
        <button class="btn btn-primary" style="width: 100%; text-align:left" type="submit">UPDATE BLOG</button>
      </div>
       <div class="form-group">
        <button class="btn btn-sm btn-danger" style="width: 100%; text-align:left" id="delete-blog" data-id="{{blog.id}}">DELETE</button>
      </div>
    </form>
  </div>
</div>

</div>
<!--
<div class="text-center">
  <h2>{{name}}</h2>
  <p>{{description}}</p>
  <p>Created by {{user.name}} on {{format_date date_created}}</p>
  
  <p>
    <button class="btn btn-primary">Edit Blog</button>
  </p>
</div>
-->

<script src="./js/profile.js"></script>